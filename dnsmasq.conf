# Resolve *.budget.home to the host machine's LAN IP
address=/budget.home/192.168.0.54

# Forward all other DNS queries to Google DNS
server=8.8.8.8
server=8.8.4.4

# Don't read /etc/resolv.conf
no-resolv

# Only listen on UDP (standard DNS)
no-dhcp-interface=*
